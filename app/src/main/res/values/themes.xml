<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GF_android" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GF_android" parent="Base.Theme.GF_android" />
    <!-- Custom button style -->
    <style name="LightBlueButton">
        <item name="backgroundTint">@color/blu_logo</item>
        <item name="color">@color/green_logo</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="DemoTheme" parent="Base.Theme.GF_android">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorAccent">@color/accent_color</item>

        <!-- Defining that new color in ColorPrimaryDark -->
        <item name="colorPrimaryDark">@color/primary_dark_color</item>
    </style>


</resources>